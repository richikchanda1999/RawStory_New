name: raw_story_new
description: Raw Story Application

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: 0.12.1
  flutter_screenutil: ^1.0.2
  firebase_admob: ^0.9.2+1
  webview_flutter: 0.3.22+1
  cupertino_icons: ^0.1.2
  isolate_handler: 0.3.1
  rxdart: 0.24.1
  flutter_html: 1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: 0.7.4

flutter_icons:
  ios: true
  android: "launcher_icon"
  image_path: "assets/launcher/download.png"

flutter:
  uses-material-design: true

  assets:
    - assets/Images/